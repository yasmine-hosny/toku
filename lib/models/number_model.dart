class NumberModel {
  const NumberModel(
      {required this.jpName,
      required this.enName,
      required this.image,
      required this.sound});
  final String jpName, enName, image, sound;
}
